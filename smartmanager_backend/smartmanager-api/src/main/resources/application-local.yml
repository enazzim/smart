spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/smartmanager?createDatabaseIfNotExist=true&characterEncoding=utf8mb4&serverTimezone=Asia/Seoul
    username: root
    password: "1111"
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
    # V002 등 이미 적용된 마이그레이션 파일 수정 시 checksum mismatch 자동 보정
    repair-on-migrate: true

logging:
  level:
    org.hibernate.SQL: warn
    org.flywaydb: info

smartmanager:
  security:
    jwt:
      secret: local-dev-secret-key-minimum-32-characters!!
      expiration-minutes: 480
    admin:
      login-id: admin
      initial-password: Admin123!
      enabled: true
  board:
    files-dir: ../../filedownload
    max-file-size-bytes: 104857600
    allowed-extensions: pdf,doc,docx,xls,xlsx,png,jpg,jpeg,zip
  backup:
    dir: ../../backup
    # mariadb-bin-dir: (선택) PATH에 mariadb-dump/mariadb가 없을 때만 MariaDB bin 폴더 지정
  drawing:
    storage-dir: ../../drawing-storage/pdf
    max-file-size-bytes: 104857600
